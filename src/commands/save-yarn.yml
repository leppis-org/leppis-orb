description: Save yarn cache

parameters:
  version:
    description: Version for bumping the cache key
    default: 'v1'
    type: string

steps:
  - save_cache:
      paths: [node_modules]
      key: deps-<< parameters.version >>-{{ checksum "yarn.lock" }}
